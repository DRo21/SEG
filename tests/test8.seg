bool a = true;
bool b = false;
bool c = a && b;
bool d = a || b;
bool e = a ^ b;
bool f = !a;

int x = 5;
int y = 10;
int z = x && y;
int w = x || y;
int v = x ^ y;

int m = (x + y) && (y - x);
int n = (x * 2) || (y / 2);
